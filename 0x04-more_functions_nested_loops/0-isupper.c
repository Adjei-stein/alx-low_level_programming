#include "main.h"

/**
 * _isupper - checks for uppercase letters
 * @c: character to check
 *
 * Return 0 or 1
 */

int _isupper(int c)
{
	int r = 1;
	int s = 0;

	if (c >= 65 && c <= 90)
	{
		return (r);
	}
	else
	{
		return (s);
	}
}
