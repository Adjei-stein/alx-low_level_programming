#include "main.h"
#include <stdio.h>
/**
 * _isupper - check the code.
 *
 * Return: Always 0.
 */
int _isupper(void)
{
	int r;
	int s;

	r = 1;
	s = 0;
	if (c == 'A')
	{
		printf("%d", r);
	}
	else if (c == 'a')
	{
		printf("%d", s);
	}
	return (0);
}
